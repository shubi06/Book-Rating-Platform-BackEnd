apiVersion: v1
kind: Service
metadata:
  name: bookrating-api-service
  namespace: bookrating
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: bookrating-api
